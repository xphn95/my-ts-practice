// 字符串
const str: string = 'hello'

// 数字
const num1: number = 123
const num2: number = NaN
const num3: number = Infinity
const num4: number = 0x246541

// 布尔值
const b1: boolean = true
const b2: boolean = false

// undefined
const uni1: undefined = undefined

// null
const nu1: null = null
// nu1 = uni1 X

// symbol
const s1: symbol = Symbol(20)

// bigint
const big1: bigint = BigInt(20)

console.log(str, num1, num2, num3, num4, b1, b2, uni1, nu1, s1, big1)
